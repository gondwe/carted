{% extends 'template/app.twig' %}

{% block body %} 

<div class="m-4">

    <div class="row">
        
        {% for index, product in products %}
        <div class="col-md-4 col-sm-6">
            {% include "pages/products.twig" with {product:product} %}
        </div>
        {% endfor %}
        
    </div>
    
</div>

{% endblock %}    
